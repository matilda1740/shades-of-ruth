import React from 'react'
import './Delivery.css'

export default function Delivery() {
    return (
        <div className="delivery_page">
            {/* 
            FORM 
            IF USER LOGGED IN GENERATE DELIVERY AMOUNT FROM INFORMATION
            
            ELSE
            
            REQUEST USERS TO SIGN UP/ ENTER ADDRESS INFO ONLY
            
            */}
        </div>
    )
}
